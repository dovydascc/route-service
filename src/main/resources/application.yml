routeService:
  integration:
    countriesEndpoint: https://raw.githubusercontent.com/mledoze/countries/master/countries.json
spring:
  jackson:
    visibility:
      field: any # Allow Jackson to serialize DTOs without default constructor. We want constructors defined by our domain, or all-args constructor.
logging:
  level:
    com.company.route: info
